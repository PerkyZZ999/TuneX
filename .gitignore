# Build
/target/
/build/
CMakeUserPresets.json
CMakeCache.txt
compile_commands.json
cmake-build-*/

# Packaging artifacts
packaging/aur/src/
packaging/aur/pkg/
packaging/aur/tunex/
packaging/aur/*.pkg.tar*
*.pkg.tar*

# Secrets and machine-local analysis
.env
.env.*
!.env.sonar.example
.scannerwork/

# cxx-qt QML language-server config (absolute local paths, regenerated by builds)
**/.qmlls.ini

# Cursor local artifacts (tracked rules in .cursor/rules/ stay)
.cursor/plans/
# Penpot MCP key lives here — never commit
.cursor/mcp.json

# OS / editors
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.idea/
.vscode/
*.code-workspace
*.user

# Coverage extras (rust-tc already writes under target/)
*.profraw
*.profdata
lcov.info
/coverage/

# Local scratch. Never commit a private music library, third-party album
# artwork, API keys, or one-off generation scripts.
/scratchpad/
*.local.png
