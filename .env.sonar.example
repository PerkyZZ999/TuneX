# Example Sonar credentials file.
# Copy to `.env.sonar` and fill in a project analysis token.
# Never commit `.env.sonar`.

export SONAR_HOST_URL="http://localhost:9000"
export SONAR_TOKEN="sqp_replace_me"
