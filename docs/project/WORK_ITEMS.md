# Work items (current slice: S13 — Lyrics, profiles, MusicBrainz)

> S1 done (W-001a/b–W-010). S2 done (W-011–W-018). S3 done (W-019–W-025). S4 done (W-026–W-031). S5 done (W-032–W-037). S6 done (W-038–W-045). S7 done (W-046, W-047). S8 done (W-048–W-051). S9 done (W-052–W-055). S10 done (W-056–W-057). S11 done (W-058–W-060). S12 done (W-061–W-063). S13 done (W-064–W-066).

## Current slice
S13 — local lyrics overlay, named library profiles, opt-in MusicBrainz. **Done 2026-09-13** (W-064–W-066). Does not reopen S12.

**Done condition:** Now Playing lyrics toggle from sidecar/embedded tags; Settings profile switch; MusicBrainz default off, missing-only.

## Queue (S13)
- [x] W-064 — Local lyrics overlay on Now Playing: sidecar `.lrc` wins, else lofty unsynced; synced lines follow position; unsynced is a scroll pane; toggle, not a tab farm; no providers (R-022, L-010).
- [x] W-065 — Named library profiles (`profiles/<id>/library.db` + own `library_roots`); Settings switch; no accounts; active id in `config.toml` (R-022).
- [x] W-066 — Opt-in MusicBrainz / Cover Art Archive, default off; worker + timeout + XDG cache; fill missing tags/art only; D-010 extra source; D-014 product-loop exception (R-022, L-009).

## Queue (S12)
- [x] W-061 — Schema v7 `play_history` + `indexed_at`; append on URI advance (restore does not count); Home Recently Played rail (R-021).
- [x] W-062 — Smart playlists: stored rules (added_days / never_played / artist / genre / composer, exclude missing); evaluate in SQL on open/play; GlassDialog rule builder on Playlists (R-021, L-004).
- [x] W-063 — Tag editor GlassDialog (title/artist/album/track/disc/year/genre/composer); lofty write on a worker then upsert that path; empty fields unchanged; permission/read-only fail loudly (R-021, L-005, R-NFR-05).

## Queue (S11)
- [x] W-058 — ReplayGain from lofty + GStreamer tags; `volume` element downstream of user volume; off/track/album; missing tags = unity (R-020, L-001).
- [x] W-059 — 0–12 s volume envelope on the existing about-to-finish handoff; not a second decoder; reduce-motion does not disable audio fade; 0 s = gapless cut (R-020, L-002).
- [x] W-060 — Enumerate PipeWire/GStreamer sinks on a worker; persist device id; default System; changing device does not drop the queue; D-005 updated (R-020, L-013).

## Queue (S10)
- [x] W-056 — Drag-reorder rows onto `moveItem` (same model diff path as insert/remove); queue-row menu Play next / Move to end / Remove; playing-row equalizer tick, static bars when `reduce_motion` (R-010, L-003 named UX).
- [x] W-057 — Persist ordered URIs + cursor in SQLite (`playback_queue` schema v6); restore paused like last-track; missing files stay dangling; no auto-play (R-010, promoted L-012).

## Queue (S9)
- [x] W-052 — Album landing page: large art header, one primary Play, meta, virtualized tracks, more-by-this-artist row; history stack grid → detail → back (R-007, R-019).
- [x] W-053 — Artist landing page: header Play, album grid, then tracks; artist card activation opens detail (does not enqueue) (R-007, R-019).
- [x] W-054 — Genre and composer browse under Your Library; `FacetListModel` over existing `genres` / `tracks.composer`; Unknown stays Unknown (R-019, promoted L-014).
- [x] W-055 — Persist recent search queries locally (cap 10); Tab / Shift+Tab cycles Songs → Albums → Artists groups; album/artist hits open landing pages (R-008, R-014, R-019).

## Queue (S8)
- [x] W-048 — Persist window geometry (min 960×640) and last library tab + sort chips in additive TOML; restore on startup; debounce geometry writes (R-015). Verified: `WindowConfig` width/height/x/y + `ViewConfig` tab/sort keys; clamp on load; `TrayController`/`LibraryManager` slots; App restores then saves after a 400 ms quiet window.
- [x] W-049 — Scrub on MiniPlayer and QueuePanel via existing `QueueModel.seekMs`; missing-file “Show in folder” (Rust `xdg-open` of the parent) and “Remove from library” (index row delete, playlists dangle per D-009) on `TrackMenu` (R-011, R-NFR-05).
- [x] W-050 — Keyboard: arrows and j/k move list/grid currentIndex; Enter plays or opens; Songs type-to-select (prefix, 400 ms reset); Settings → Shortcuts is a complete accurate map (R-014). Not L-007 rebind.
- [x] W-051 — First-run hero primary stays disabled “Scanning…” until the first album row exists, then “Play Something”; overlay motion already on the 200 ms budget through `Appearance.duration()`; compact/HiDPI token tweaks deferred unless a clip is found.

## Queue (S7)
- [x] W-047 — Density pass: smaller, crisper chrome without dropping the 12px caption floor or 44px hit targets (R-018). Verified: `docs/DESIGN.md` + `Theme.qml` stepped type (display 32→28, headline 22→18, title 16→14, body 14→13), spacing (`lg`/`xl`/`xxl`), and shell geometry (rail 216, panel 288, mini-player 64, track rows 48, art 280 / play 52). Hardcoded 56/64/76/320 sizes in QML now read Theme tokens. 44px targets and the 40px dense-row exception stay. Caption floor remains 12px. Gates recorded in VALIDATION.md.
- [x] W-046 — Tray icon + Settings refine + close-to-tray + type-scale −2px + scanned folders in Settings (R-002, R-015, R-018). Verified in code: `tunex-app::tray` serves `org.kde.StatusNotifierItem` on a detached thread (same shape as MPRIS/notify); host tooltip carries title/artist on hover; Activate/ContextMenu open `TrayPopup` (play/pause, previous, next, now-playing, Show/Quit, Esc, 44px targets, Lucide glyphs, DESIGN.md surfaces). Close-to-tray defaults **on**, persists in `[window] close_to_tray`, and hide-on-close requires a live tray host so a missing watcher never leaves a headless process. Settings is the IA list-detail (Library / Playback / Appearance / Shortcuts) bound to `LibraryManager` / `QueueModel` / `TrayController` — no lorem. Scanned-folder add/remove/rescan moved here; `FoldersDrawer` removed; Home/Library “Add folder” CTAs open Settings → Library (native `FolderDialog`); the sidebar Folders item is **not** the management surface (Folder browse is a separate rail destination). Type scale reduced ~2px in `docs/DESIGN.md` tokens and `Theme.qml` (body 16→14; caption stays 12px floor). Gates recorded in VALIDATION.md.

**Pre-S6 visual contract (2026-09-10):** the shell was refactored to follow `docs/mockup.png` + `DESIGN_BRIEF.md` adaptation (real models only) *before* S6 polish. W-038–W-042 start from that shell; do not treat the prior glass WIP as a completed W-038.

## Queue (S6)
- [x] W-038 — Glass hierarchy audit + restraint pass per SPEC §21 (R-018 final part). Verified: glass is now hierarchical per `DESIGN.md` — strong glass on Now Playing only, subtle glass on the drawers/dialogs/menus (new `GlassBackdrop` stack: shadow → canvas snapshot → blur → 70% tint → 1px edge, snapshotted once per show via `live: false`, with sliding hosts passing their resting geometry so the snapshot never samples off-screen), and rows/rail/cards/mini-player opaque. Blur is calibrated, not guessed: `MultiEffect.blurMax` is not the CSS scale, so KWin measurement fixed `blurMax 64` ≈ CSS 16px (subtle) and `×1 multiplier` ≈ CSS 32px (strong). New shared controls `GlassMenu`/`GlassMenuItem`/`GlassMenuSeparator`, `GlassDialog`, `PlayButton`, `ProgressSlider`; `Glass` singleton became `Appearance` (canvas + reduce-motion/transparency + `duration()`). Icons are one Lucide-derived line family drawn with `Shape`/`PathSvg` (`LICENSES/Lucide.txt` + ISC/MIT in the PKGBUILD). Shadows collapsed to the documented scale (overlay/hero, cards none) and glow appears exactly twice (playing transport button, selected-nav bar). Restraint fixes: one primary action per view (Playlists sidebar button demoted), disabled buttons fade as a whole control, focus rings follow keyboard focus only, Esc closes Now Playing (Popup needed `focus: true`), repeat/mute/shuffle toggles show real checked state. Shell fix found by the audit: a rail playlist entry opened the Playlists view without selecting the playlist (a list delegate resolves `root` but not other document ids — `App.qml:484 ReferenceError`), now routed through a root function with required delegate properties. KWin isolated sessions at 1200×800 (compact: mini-player + glass queue drawer) and 1600×1000 (wide: opaque docked queue), plus a reduced-transparency run proving the opaque fallback keeps the layout. Evidence `docs/project/evidence/w038-*.png` (compact drawer, Now Playing, dialog, folders drawer, menu, home icons, focus ring, wide docked queue, two reduced-transparency shots). Gates: `scripts/qml-lint.sh` 32/32 ✓, CMake build ✓ (Qt 6.9 floor for `RectangularShadow`), `rust-tc doctor` ✓, `makepkg --printsrcinfo` in sync + `namcap` clean, app log clean in every run.
- [x] W-039 — Transitions/micro-interactions within motion budgets + reduce-motion honored everywhere (R-018 final part). Verified: every animation now goes through `Appearance.duration()` against the `DESIGN.md` budget — hover/focus 120 ms colour-only (track rows, nav items, chips, icon buttons, primary buttons, menu items, the transport disc, card hover overlays, and the playlist sidebar rows, which previously had click but no hover feedback at all), row insert/remove 160 ms in Up Next, drawer/overlay 200 ms, keyboard highlight moves inside the hover budget (nine hardcoded 120 ms values routed through the budget so reduce-motion can zero them), and progress still follows the engine with no easing. Row transitions needed the model to stop shouting: `QueueModel` reset on every change, which rebuilds every delegate and cancels row animations, so it now diffs its rows and emits `beginInsertRows`/`beginRemoveRows` for the single-row cases — walking the rows back to the announced shape for the "about to change" half of the pair, as Qt expects — and keeps the reset for cursor moves and bulk enqueues. Six diff tests cover appended/mid-queue inserts, removal, cursor moves and album-sized adds. KWin frame captures prove the motion: a queued row fades in (semi-transparent at 60 ms, settled by 150 ms) while its neighbour stays put, a removed row fades out, a hovered row tints within the budget, and with `reduce_motion` the same row is already solid at 40 ms and the drawer is fully open at 30 ms. Evidence `docs/project/evidence/w039-*.png`. Gates: `scripts/qml-lint.sh` 32/32 ✓, CMake build ✓, `rust-tc doctor` ✓, app logs clean in every run.
- [x] W-040 — Artwork backgrounds + placeholder polish (R-018 final part; closes R-005). Verified: the artwork cache built in S2 had no caller — covers never reached the UI. New `tunex-app::art::ArtCore` resolves them on a worker thread (embedded bytes first, then folder art per D-010), deduplicated per token and answered as cache paths, so no decode ever touches the UI thread. Albums carry the track their cover comes from (`MIN(tracks.path)` joins it into the album projection), the grid and the home rail ask per card as it appears and pump a short timer that stops itself, and each cover lands as `dataChanged` on one row rather than a reset that would rebuild the grid. New shared `Artwork.qml` draws every cover — monogram underneath, cover faded in over it at 120 ms, broken or missing sources simply never arrive — and now backs the album cards, the mini-player, the docked summary and the Now Playing crest (square at the card radius with art, the one circular monogram crest without). Now Playing's backdrop is now the track's own cover, cropped and blurred under the tint, with the blue bloom kept only as the no-cover fallback; the placeholder banding the W-038 audit noted is gone with it. MPRIS publishes `mpris:artUrl` from the same cache, closing the W-021/W-032 art deferral. KWin runs against the tagged demo library (embedded/folder/none/broken mix): a cold cache fills the grid within a second, the broken cover (ASCII bytes named `cover.jpg`) and the art-less album keep their monograms with no crash, a restart shows art immediately from the 7-entry cache, and `qdbus6` reports `mpris:artUrl` pointing at the cached 512 px thumbnail. Evidence `docs/project/evidence/w040-*.png`. Gates: `scripts/qml-lint.sh` 33/33 ✓, CMake build ✓, `rust-tc doctor` ✓ (226 tests), app logs clean.
- [x] W-043 — Adopt the `QtQmlBestPractices` qmllint plugin in the QML gate (mid-slice addition, requested 2026-09-11). Verified: `scripts/qml-lint.sh` now discovers the plugin (sibling checkout → installed `qmllint` plugin dir → `TUNEX_QMLLINT_PLUGIN_PATH`, `off` to skip) and enables all eight `Plugin.BestPractices.*` categories at `warning`, which `--max-warnings 0` turns into gate failures. Discovery rather than a hard requirement because the plugin is a local project with no published remote, and CI runs in a clean Arch container: found means the categories gate, absent means the baseline lint still runs and the script names the categories that went unchecked. Both branches proven by a temporary `property var` probe — the gate failed with the plugin and passed with `TUNEX_QMLLINT_PLUGIN_PATH=off`. The plugin found 36 real findings, all fixed: 31 `import TuneX 1.0` version tails dropped, `App.qml`'s `property var history` became `property list<string>` (slice/concat/index all still work), `LibraryView`'s tab strip stopped modelling JS objects (`["songs","albums","artists"]` plus a `tabLabel()` function and `required property string modelData`), and the Playlists sidebar delegate replaced three mutable `property x: model.x` projections with `required property` reads — which meant declaring `required property int index` too, since a delegate with required properties no longer receives the context properties. KWin isolated session at 1600×1000 against the real 530-track library: tab chips render and switch, two created playlists render name + song count and select by row, back/forward walk the history, app log clean. Gates: `scripts/qml-lint.sh` 33/33 ✓ with the plugin, CMake build ✓, `rust-tc doctor` ✓ (229 tests).
- [x] W-041 — Profile pass: QML profiler + tracing, 50k scroll/search observation, numbers recorded vs M6 targets (R-NFR-06, measure not gate). Verified: every SPEC §25 target measured on real instruments — Qt scene-graph frame timing (`qt.scenegraph.time.renderloop`), the app's own `app.ready` startup log, two new benchmarks (`bench_library` generates a 50k index through the real upsert path; `bench_search` reports p50/p95/max per query shape and times the FTS5 stage separately), and `scan_probe` against the user's real 530-track library. Results vs targets: **cold start** 850 ms windowed at 50k / 397 ms offscreen (target <1.5 s) ✓; **search p95** 6.8–9.2 ms across five query shapes at 50k (target <50 ms) ✓, after the profile pass found and fixed a ~1.0 s p95; **scroll** 132 frames over two flick-scrolls of the real artwork grid at p50 16 ms / p95 17 ms with app-side work (sync+render) averaging 0.36 ms and exactly one frame of 132 missing the budget for an app reason (target 60 fps) ✓; **controls** the transport icon had already flipped by the first capture 8 ms after the click, and a track-row click produced the full queue + transport + now-playing update between 16 and 33 ms (target <50 ms) ✓; **no UI stalls during scans** every frame during a 329 ms cold scan of 536 files rendered with sync=0 render=0 (the one 168 ms outlier is the app's first frame ever, which a no-scan session reproduces at 149 ms) ✓; **scan throughput** 530 files indexed cold in 607 ms ≈ 52,400 files/min (target >500) ✓; **artwork never blocks the UI** covers resolved on the `ArtCore` worker while frames stayed at 0 ms app-side ✓; **database work off the UI thread** architectural (scan worker owns its connection, search debounced onto a worker, art resolved on a third) ✓. Also recorded: RSS 181 MB at 50k / 224 MB on the real library with a warm artwork cache, a 30 MB index for 50k tracks, and the 4,167-album grid fully populated within the 50 ms frame capture after the view switch. Two optimizations were justified by these measurements and landed as their own commits (covering indexes for the grouped joins, and rescans that stop re-reading unchanged files), along with a concurrent-migration defect the schema change exposed. Caveats recorded honestly: the 50k index is synthetic rows with no audio files, so scroll-with-artwork was measured on the real library instead; the frame numbers come from an Arc B580, not the iGPU the target names; and EIS wheel events do not reach the app in a nested KWin session, so scrolling was driven by drag-flick (a harness limitation — the drag proved the grid scrolls normally). Evidence in VALIDATION.md. Gates: `rust-tc doctor` ✓ (229 tests), CMake build ✓, `scripts/qml-lint.sh` ✓, app logs clean in every run.
- [x] W-044 — Design/UX refinement pass against `docs/mockup.png` (mid-slice addition, requested 2026-09-11). Verified: audited every surface against the mockup and `DESIGN.md` first, which found the glass hierarchy already correct from W-038 (strong glass on Now Playing only; subtle glass on dialogs, menus and drawers; rows/rail/cards/mini-player opaque) — so the real gaps were elsewhere. Fixed: album, playlist and artist cards were not cards at all (artwork and text floating on the canvas) and now carry the mockup's surface step plus a 1px hairline, no shadow; the hover state was a 45%-opacity *darkening* veil where `DESIGN.md` specifies "a 4% brighten + play-affordance overlay button", now a new shared `PlayBadge`; the two "See all" affordances were bare `Text` with a text-sized `MouseArea` (~50×18px hit area, no hover feedback, unreachable by keyboard) and are now a shared `TextLink` with a 44px target, hover colour and a focus ring; artist cards were entirely inert — a grid of crests that did nothing — and now play the artist through the already-exposed `enqueueArtist`, with the badge stating what the click does. Mockup alignment, using only features TuneX already has: the hero carries two actions like the mockup (primary Continue/Add folder plus a secondary "Browse library" reusing the existing browse signal, hidden in the empty state so one primary stays one primary), the top-bar history buttons stand as circles, the Playlists rail became the mockup's wide tile-beside-text card so the two Home rails read as different kinds of thing, the Now Playing title/artist left-align under the artwork, and both rails now size their cells so whole cards fill the row instead of clipping one mid-card at the panel edge. Nothing from the mockup that TuneX does not own was invented — no favourites heart, lyrics/About/Related tabs, device picker, bell, avatar or algorithmic rails (`DESIGN.md` Do's and Don'ts). Verified in KWin isolated sessions against the real 530-track library at 1282×830 and, resized through KWin scripting, at 1102×762: cards, hover badges and rails render correctly at both, the compact width drops the docked panel for the mini-player and reflows the rails to four whole cards, and the app log is clean. Evidence of that pass was captured against a private library and is not in the public tree; W-042 demo-library shots cover the same chrome. Gates: `scripts/qml-lint.sh` 35/35 ✓, CMake build ✓, `rust-tc doctor` ✓ (229 tests).
- [x] W-045 — Charcoal palette, translucent chrome, artwork trimming, and generated assets (mid-slice addition, requested 2026-09-12). Verified: the neutrals were navy-cast, so a blue accent dissolved into a blue room; they are hue-free charcoal now (`#0B0B0E` canvas up) with royal blue `#2B5CE6` as the only chromatic family, `accent-secondary` moved off cyan into that family, and every token re-measured (foreground 17.9:1, muted 7.9:1, accent 7.3:1, white on primary 5.6:1 — all AA). Chrome (rail, top bar, docked player) is tinted-translucent rather than blurred, because permanent chrome under a live backdrop blur would cost a blur pass every frame; real blur stays with overlays that appear and leave. A new `AmbientWash` gives the translucency something to reveal — the playing cover, blurred to abstraction, desaturated, held at 14% and faded down the view, repainting on track change rather than per frame. Reduced transparency was probed live: surfaces return exact tokens with zero blending. Thumbnails now trim uniform letterbox/pillarbox padding under a narrow rule (one axis, both sides, same flat colour, each bar ≥2% of the edge, ≥half surviving; `orig` untouched) — 8 of 13 covers in the real library trimmed, and the one padded cover it refuses has a watermark printed across its bottom bar, which is the rule working. Four assets generated with `openai/gpt-image-2.5-sunburst` and committed with provenance files: `hero-charcoal.png` (replaces the blue `hero-night.png` that fought the new palette), `art-placeholder.png` (texture *under* the monogram, never instead of it — the letters are what distinguish art-less albums), and `empty-library.png` / `empty-search.png`, whose solid backdrops were keyed to alpha so they do not show as squares over the ambient wash. Total asset weight fell from 3.18 MB to 3.0 MB. Two defects fixed on the way: a latent binding loop in the songs-tab footer that only fires once a library is big enough to show the 500-song cap notice, and a first empty-state generation that came back neon with a bloom halo, against the rule that glow appears exactly twice. Gates: `rust-tc doctor` ✓ (236 tests), `scripts/qml-lint.sh` 36/36 ✓, CMake build ✓, `design.md lint` 0 errors ✓, app logs clean.
- [x] W-042 — S6 gate: release checklist + full DoD; close or loop back. Result: PASS, no loop-back. SPEC §39 walked end-to-end in an isolated KWin session (`isolate_home`, tagged demo library 8 albums × 6 tracks with the embedded/folder/none/broken art mix, scratch XDG, never the user's `~/Music`): add/scan 53 files → 48 tracks without a UI stall (startup 390 ms, scan finished before the first frame was useful) · browse Home/Albums/Artists/Songs · search `nova`/`harbor` groups instantly · artwork + monogram-over-placeholder · play Blue Hour into the docked queue with ambient wash · Now Playing strong glass · Evening playlist create via the glass name dialog · Folders drawer subtle glass ("Up to date · 48 tracks from 53 files") · hover PlayBadge on artist cards · glow only on the playing transport and the selected-nav bar. Offline: `scripts/netoff-rehearsal.sh` ALL GREEN (restore + play/pause/volume + corrupt `Notify`). Headless DoD: `scripts/dod-demo.sh` 5/5. S6 acceptance holds from W-038–W-041 (glass hierarchy, motion budgets, artwork pipeline, measured M6 numbers — not a gate). Two W-045 judgement calls confirmed by the owner and re-proven here: the placeholder texture stays *under* the letter, and glow stays the two sanctioned uses (empty-state prompts still forbid bloom). Evidence `docs/project/evidence/w042-{home,albums,artists,search,playing,now-playing,playlist,folders}.png`. Gates: 236 tests ✓, `rust-tc doctor` ✓, `sonar` ✓ QG OK (81.9% coverage, 0 violations), `scripts/qml-lint.sh` 36/36 ✓, CMake build ✓, `design.md lint` 0 errors ✓, `makepkg --printsrcinfo` in sync + `namcap` PKGBUILD clean + `desktop-file-validate` clean. App log clean of product errors (isolated-session portal + AT-SPI adaptor noise only).

## Done (S1–S5)
- [x] W-032 — MPRIS engine wiring: PlaybackStatus/Position/Volume/Metadata from the real engine + controller (track id/title/artist/album/art URL, CanSeek/CanControl, PropertiesChanged on AppEvent); seek/volume methods live (R-013 full, S1-skeleton wiring deferred here by design). Verified: `MprisHub` snapshot (status/position/volume/shuffle/repeat/track/caps) published by the `QueueModel` poll + `MprisCommand` inbox drained onto the controller (D-007: bus thread never touches Qt/engine); `PropertiesChanged` on non-position moves, `Seeked` on explicit seeks, stale `SetPosition` ignored; honest idle (empty queue stays Stopped); art URL stays `None` (queue art still placeholder per W-021 deferral). 205 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (81.2% new coverage, 0 violations — S3776 split into transport/value arms + serve helpers); `dod-demo.sh` 5/5 ✓ (smoke now asserts honest idle + busctl volume round-trip from a scratch XDG home). Full `playerctl` matrix → W-033.
- [x] W-033 — MPRIS full-matrix verification: play/pause/stop/next/prev/seek/volume/metadata/status via `playerctl` (`qdbus6`/`busctl` substitution where the binary is absent) + KDE widget check (R-013, R-017). Verified: offscreen binary + scratch XDG homes, `playerctl` absent so `qdbus6`/`busctl` carried the matrix per the item. Root: Identity=TuneX, DesktopEntry=tunex, schemes=[file], 6 audio mimes, CanQuit/CanRaise/HasTrackList false. Empty queue: PlayPause stays Stopped (honest idle), Metadata empty (`a{sv} 0`), CanPause/CanSeek/CanGoNext denied. Trackful via last-track restore (sine.wav paused at 1.5s): Play→Playing, Pause→Paused, PlayPause toggles both ways, Seek +1s lands/clamps, valid SetPosition→2.0s, stale SetPosition ignored at 2.0s (MPRIS §2.2), Volume 0.5 round-trip, Shuffle true, LoopStatus Playlist, Next/Previous hold Playing under repeat-all single track, Stop→Stopped, OpenUri parked-Ok with no state change. Metadata carries trackid `/org/mpris/MediaPlayer2/track/q0` + title/url/length; art URL stays `None` (W-021 deferral stands). KDE widget check by property contract (no live Plasma host offscreen): Identity/PlaybackStatus/Metadata/Position/Volume/Can* all served. 205 tests ✓, `quick` ✓ (full `doctor`+`sonar` at W-037 gate).
- [x] W-034 — Desktop notifications over D-Bus (track change + playback errors), minimal and non-intrusive, offline-safe (R-017, SPEC M5). Verified: `tunex-app::notify` (`Notify` via session bus, transient hint + 5 s expiry, `audio-x-generic` icon; no server/no bus → debug log, never an error; detached thread so the Qt poll never blocks); `QueueModel` toasts on track-URI advances only (restore seeds the cursor silently, stops/repeats quiet)   + on drained `PlaybackError` events (sync UI errors like empty-queue Play stay untoasted). 209 tests ✓ (4 new notify unit + restore-seeding assert), `doctor` ✓, `sonar` ✓ QG OK (81.0% new coverage, 0 violations), `dod-demo.sh` 5/5 ✓. KWin visual toast proof deferred to the W-037 gate (no notification daemon offscreen).
- [x] W-035 — PKGBUILD final: `package()` install rules + Corrosion vendoring for chroot builds + clean-chroot `makepkg` + namcap clean (R-016 final; closes W-001b deferrals). Verified: `package()` installs binary + desktop entry + 256px icon + GPL-3.0 text (new `LICENSE` + `packaging/aur/tunex.{desktop,png}`, desktop-file-validate clean); depends now cover all 6 matrix formats (good/bad/ugly/libav) + hicolor-icon-theme; `options=('!lto')` (makepkg LTO breaks the cxx-qt mixed static link with `cxxbridge1$…` undefined symbols — root-caused via a real `makepkg -f` failure); `cargo fetch --locked` in `prepare()`; chroot-network finding: clean chroots DO have network (FetchContent/cargo fetch work there), so no vendoring — the old "no network" note is corrected. Full local `makepkg -f` green (`tunex-0.1.0-1` tarball: bin/desktop/icon/license all present); `namcap` PKGBUILD clean, tarball has only benign findings (bundled-QML false positive; gst plugins invisible to static analysis but proven by the codec matrix). `makepkg --printsrcinfo` regenerated. True `extra-x86_64-build` still needs a rooted maintainer run — recorded follow-up, not a code risk.
- [x] W-036 — Wayland + X11 verification pass + high-DPI + native dialogs check (FoldersDrawer FolderDialog both sessions) (R-017, R-NFR-02/04). Verified: Wayland isolated sessions — shell/views/drawer render; native `FolderDialog` opens, browses the real FS, selects rows, Ctrl+L location entry, Esc cancels cleanly (no crash, logs clean); seeded-config startup scan yields 7/7 fixture rows with "Up to date · 7 tracks from 7 files" + folder row in the drawer. X11: `QT_QPA_PLATFORM=xcb` under session-local XWayland launches, registers its window, and exposes the complete AT-SPI tree (all views/transport/queue controls named); pixels stay black through nested XWayland (both RHI and software backends) — harness raster limitation, true X11-Plasma visuals stay maintainer follow-up. High-DPI (`QT_SCALE_FACTOR=2` @2560×1600): crisp 2x render, layout holds, no overlap. 7 evidence shots `docs/project/evidence/w036-*.png`. Environment limits (no code impact): dialog Open… accept-click won't fire via EIS and keyboard focus can't land on it, so folder-add completion ran via seeded config; dialog open/browse/select/location are proven.
- [x] W-037 — S5 gate: network-off DoD rehearsal + `playerctl` matrix + Wayland/X11 sessions + clean-chroot PKGBUILD; close or loop back. Result: PASS, no loop-back. Network-off: new `scripts/netoff-rehearsal.sh` runs the app in `bwrap --unshare-net` (TCP refused) — bus owned, sine.wav restored paused, Play→Playing/Pause→Paused, volume round-trips (0.50000122 f32 precision noted), corrupt playback emits `Notify(Playback error)` on the bus, app survives — ALL GREEN. Matrix (W-033), notifications emission (W-034 path proven here), Wayland/X11/HiDPI/dialogs (W-036), local `makepkg -f` + namcap (W-035) all hold. Gates: 209 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (81.0% new coverage, 0 violations), `dod-demo.sh` 5/5 ✓ + netoff ALL GREEN ✓. Maintainer follow-ups (no code risk): true `extra-x86_64-build`, true X11-Plasma visuals, toast pixels under a real notification daemon.
- [x] W-026 — MiniPlayer persistent transport: art thumb, title/artist, play/pause, next/prev, read-only progress, volume, queue toggle; 76px opaque bottom bar below 1280px (R-010, R-011 volume, R-018 functional). Verified: QueueModel currentTitle/currentArtist/durationMs/volumePct/mute + config persist; MiniPlayer bar (<1280, hidden pre-first-play) + docked QueuePanel now-playing summary (≥1280); read-only progress (no seek); 185 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (80.3% new coverage, 0 violations); qmllint/qmlformat/CMake green + offscreen smoke clean; KWin proof deferred to W-031 gate.
- [x] W-027 — Async seek (event + AsyncDone) so a FLUSH seek never wedges an EOS pipeline — required before any UI slider (R-011, W-021 follow-up). Verified: `seek` posts a flush `gst::event::Seek` and returns immediately; `AsyncDone` clears `seek_pending`; paused seek still lands 1400–1600 ms; post-EOS seek returns in under 500 ms (no `seek_simple` hang). 186 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (80.3% new coverage, 0 violations).
- [x] W-028 — Expanded Now Playing overlay: blurred art, full controls, progress scrub (depends on W-027) (R-018). Verified: `QueueModel.seekMs` posts the W-027 async seek; overlay `NowPlayingView` (MultiEffect 32px blur + 68% tint, opaque `surface` when `reduce_transparency`) opens from MiniPlayer art/title or docked/drawer now-playing row; Close/Esc; live scrub skips poll while dragged; shuffle/prev/64px play/next/repeat + volume + Up Next; reduce-motion zeros overlay duration. 188 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (80.3% new coverage, 0 violations); qmllint/qmlformat/CMake green + offscreen smoke clean; KWin proof deferred to W-031.
- [x] W-029 — Keyboard map: Space play/pause, media next/prev/volume, search `/`+Ctrl+K already live, Esc/back, documented list (R-014). Verified: Space play/pause except in text fields; media play/pause/next/prev + volume up/down/mute; `/` and Ctrl+K skip while typing; Alt+Left/Right (and Back/Forward) walk history; Esc closes Now Playing, then search unwind, then back. Settings stub lists the map. 188 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (80.3% new coverage, 0 violations); qmllint/qmlformat/CMake green + offscreen smoke clean; KWin walkthrough deferred to W-031.
- [x] W-030 — Best-effort last-track + position restore on restart when the file still exists (R-011, R-015). Verified: `PlaybackConfig.last_uri`/`last_position_ms` persist with volume/shuffle/repeat; `PlaybackController::restore_paused` loads one track paused (no auto-play) and retries seek on poll after preroll; missing files skip with no toast; second `QueueModel` restores URI + ~1500 ms. Queue-across-restart stays L-012. 195 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (81.2% new coverage, 0 violations). KWin restart proof deferred to W-031.
- [x] W-031 — S4 gate: keyboard walkthrough + offline run + a11y/contrast/blur-off check; close or loop back. Result: PASS, no loop-back. Keyboard: Space play/pause, `/`+Ctrl+K search focus, scope-aware Esc unwind, Alt+Left/Right history, settings key list — 11 walkthrough shots `docs/project/evidence/w031-*.png` (home, library, miniplayer, now-playing, overlay-closed, space-pause, search, search-esc, settings-keys, history, restore); restart restores sine.wav paused at ~0:01/0:05. A11y: contrast AA all-pass (fg 17.8, muted 7.8, primary-text 5.2, accent 11.5, error 8.3), 2px focus rings (search field, play, sliders), blur-off run with `reduce_transparency`+`reduce_motion` proves opaque `surface` fallback + instant open (`w031-blur-off.png`), focus-ring library shot (`w031-focus-ring.png`); corrupt file errors loudly while the app survives (NFR-05). Offline by construction (zero network crates in `cargo tree`). 195 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (81.2% new coverage, 0 violations); qmllint/qmlformat/CMake green. Note: Esc-close did not fire in the blur-off session (3 attempts; likely EIS focus artifact) — overlay-closed was proven in the main walkthrough run, no code change.
- [x] W-019 — Search backend: multi-term FTS5 parser (AND across 7 fields, quoted literals, prefix tails) + debounced worker (~150 ms) with generation stale-cancel + grouped results (tracks/albums/artists, LIMIT 200/group) + TrackRow hydration (R-008, R-NFR-01). Verified: multi-term AND narrows, punctuation literal, prefix stems, empty short-circuits, groups resolve with counts, dup-title ownership guard; burst coalesces to latest, stale dropped worker- and controller-side, missing→empty, corrupt→surfaced-then-cleared; 134 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (S3776 split into coalesce/supersede helpers).
- [x] W-020 — Search UI: search field + shortcut focus, results views reusing TrackRow/AlbumCard/ArtistCard, QML search models fed by controller, loading/empty/no-result states, Esc clears (R-008, R-014 part). Verified: pill field in topbar (`/` + Ctrl+K, scope-aware Esc drill→text→back, Down/Enter into results); per-model workers (submit/pollSearch/isSearching/errorText) with staggered settle flags; hint/loading/no-results/empty/error/200-cap states; album drill + back-resubmit; qmllint/qmlformat/CMake green + offscreen smoke clean, KWin proof deferred to W-025 gate; qt-qml-review (6 agents, 7 fixes); 140 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-021 — Queue depth backend: QueueItem library enrichment (track id/artist/album/duration/art key) + enqueue-from-library helpers (tracks/album/artist, play-next/play-now) + engine advance/end-of-track wiring + gapless album check (R-010 full). Verified: enriched items round-trip tags; provider advances cursor on silent handoffs; missing files skip via stat precheck with error events (no 10 s pipeline block); repeat-one never replays broken tracks; next/previous/restart wiring deterministic; gapless 2-clip album plays Loading→Playing→Stopped with cursor following; album/artist enqueue ordered, missing skipped; 155 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (S3776 split into play_pick).
- [x] W-022 — Queue UI: Up Next panel (reorder/remove/clear, row play-next/play-now actions, shuffle/repeat toggles, now-playing highlight, empty state); queue survives navigation (R-010 full). Verified: app-scoped QueueModel (poll/transport/queue ops/enqueue-by-id) + QueuePanel drawer (transport, toggles, rows, menus, Alt-reorder, error line, follow-cursor) + TrackRow actions (click-to-play, ⋯ menu, playing marker) + library/search menus + drill album buttons + Enter/Delete keys; qmllint/qmlformat/CMake green + offscreen smoke clean, KWin proof deferred to W-025 gate; qt-qml-review (6 agents, 8 fixes); 165 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-023 — Playlists backend: schema v4 (playlists/playlist_tracks + v1→v4 chain test) + full CRUD + add/remove/reorder/play-order + dangling-as-missing + restart persistence (R-012, R-006). Verified: unique non-blank names (explicit conflicts), entries hydrate with left-join (deleted dangle with link kept, missing-flag resolves), repeats distinct by entry id, dense positions + clamped moves, renames keep id links, root removal dangles, file restart round-trip; 172 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-024 — Playlists UI: sidebar section + list/detail views + create/rename/delete dialogs + add-to-playlist from rows + play-all → queue + empty states (R-012). Verified: app-scoped PlaylistModel shared with library/search TrackMenu (add-to-playlist + auto-named create) + PlaylistsView (sidebar/detail, create/rename/delete dialogs, play/queue all, Alt-reorder, dangling/missing badges, three empty states) + PlaylistTrackModel (entry CRUD) + QueueModel.enqueuePlaylist; qmllint/qmlformat/CMake green + offscreen smoke clean, KWin proof deferred to W-025 gate; qt-qml-review (Overlay dialogs, playable menu guards, header wrap at min width, menu dismiss on reset); 183 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (80.0% new coverage, 0 violations).
- [x] W-025 — S3 gate: typing observation + queue/FTS5 unit tests + gapless album listen + KWin GUI proof (search/Up Next/playlists) + offline rerun; close or loop back. Result: PASS, no loop-back. Typing: `/`/field focus, Searching… debounce, no-match empty; FTS5 12 tests ✓; gapless unit test Loading→Playing→Stopped ✓; KWin isolated home (fixture library) 12 evidence shots `docs/project/evidence/w025-*.png` (library, search states, playing + Up Next, playlists empty/create/detail, row-menu add); Evening playlist persisted 1 entry in isolated DB; offline by construction (no network crates). 183 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-011 — Metadata extraction behind a trait (`lofty` 0.24.0 impl): title/artist/album/album-artist/composer/genre/year/track/disc/duration + embedded artwork bytes (front-cover preferred); empty strings normalize to `None`, corrupt files error as `Error::Metadata` (R-004). Verified: untagged-fixture unknowns, lofty write→read round-trip (incl. BMP art bytes), corrupt path; 70 tests ✓, `doctor` ✓, `sonar` ✓ QG OK (RUSTSEC-2024-0436 `paste` advisory ignore-documented: transitive via lofty, no CVE).
- [x] W-012 — Schema v2 (artists/albums/tracks full columns, genres, folders, scan_state) + FTS5 tables + v1→v2 migration test (R-006). Verified: v1 row survives migrate with backfilled search hit; transactional upsert resolves names + refreshes index; delete cascades to FTS via trigger; prefix search BM25 + LIMIT 200; 72 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-013 — Scanner worker (`tokio`, walk → metadata → upsert, stable-key reconcile incl. renames, progress counts) (R-002, R-003). Verified: tagged scan fills all fields + search; corrupt indexes by path with failure counted; rename retargets same row; delete flags missing and return clears; `scan_folder_live` streams progress to finished; 78 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-014 — Filesystem watcher (`notify`, debounced, enqueue-only callbacks) (R-003). Verified: created file arrives in batch; 5-file burst coalesces ≤2 batches; missing root errors as `Error::Io`; callbacks enqueue only, debounce thread owns all decisions, `Drop` joins cleanly; 81 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-015 — Artwork pipeline (embedded/folder art → XDG cache, content keys, thumbnails, LRU, decode caps) (R-005). Verified: PNG caches orig+64/256/512 capped; over-dimension/corrupt/over-cap inputs → placeholder; cover>folder>front priority; embedded wins; tiny-budget LRU evicts oldest; 87 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-016 — Library QML models + browse views (artists/albums/tracks, virtualized, placeholders) (R-007). Verified: artist/album/song models load from index (missing db → empty state); songs tab capped 500 with footer; album drill-down filters in disc/track order; monogram placeholders; qmllint/qmlformat/CMake green + KWin proof deferred to W-018 gate; 111 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-017 — Library folders UI (native add/remove, scan progress) + restart persistence check (R-002, R-015). Verified: add/remove persists config + restarts watcher; remove GCs rows; watcher batch wakes follow-up scan; restart restores folders + rows; drawer with progress/loading/error states; 118 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-018 — S2 gate: 12k-file scan observation + rename/delete fault injection + offline rerun; close or loop back. Result: PASS, no loop-back. 12k files → 12k rows in 2.5 s, 0 failures; 100 renames reconciled without dupes, 100 deletes flagged missing, restart idempotent; offline by construction (zero network deps); KWin DoD green with 5 evidence shots. 118 tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-001b — CMake top-level (Qt 6.8+, Corrosion v0.6.1 pinned, `qt_add_qml_module` URI `TuneX` + `App.qml` shell) + PKGBUILD skeleton (url + git source `github.com/PerkyZZ999/TuneX`, namcap clean). Verified: configure warning-free, full build green (Corrosion Rust + QML module), `tunex` binary runs, `qmllint`/`qmlformat` clean, `printsrcinfo` OK. Full `makepkg` + chroot deferred to S5 (needs executable + Corrosion vendoring). (R-001, R-016)
- [x] W-002 — cxx-qt bridge proof (pinned: cxx 1.0.198, cxx-qt/-lib/-build 0.10.0): `TrackListModel : QAbstractListModel` (Title/Artist roles, rowCount/data/roleNames overrides, append/clear invokables) + Rust `main` booting Qt + proof `App.qml`. Verified: 10 unit tests ✓, KWin GUI round-trip 3× (button → Rust append → rowsInserted → row rendered, screenshot-observed), `doctor` ✓, `sonar` ✓ (0 violations; coverage waiver below). (R-001)
- [x] W-003 — app window + nav skeleton + theme tokens + tracing + XDG config: `Theme.qml` singleton (42 DESIGN.md tokens), shell (`App.qml` + rail/topbar/history, `HomeView`, `SectionStub`, `NavItem`), `tunex-core::config` (TOML schema + clamp + round-trip/compat tests), structured `tracing` boot. Verified: 15 unit tests ✓, KWin GUI (themed shell renders, rail nav + history proven, evidence shots), `doctor` ✓, `sonar` ✓ (0 violations; coverage waiver stands). (R-001, R-015)
- [x] W-004 — PlayerEngine over `playbin3` (gstreamer 0.24.5, tokio 1.53.1): load/play/pause/stop/seek/volume/mute/position/duration + bus thread → `PlayerEvent` channel (re-queried truth, deduped emits, try_send so async callers never panic). Verified: 14 engine tests (fakesink, missing-file sync+async error paths) ✓, `doctor` ✓, `sonar` ✓ (S3776 complexity fixed by handler split; 0 violations; coverage waiver stands). (R-009, R-011)
- [x] W-005 — In-memory queue + about-to-finish preload + next/prev/shuffle/repeat (R-010): `Queue` (order/cursor/history, hash-chained shuffle, repeat modes, idempotent peek) + 15 pure tests; engine preload hook with queued-URI dedupe; WAV gapless integration test (Loading→Playing→Stopped, no intermediate stop). Verified: 46 workspace tests ✓, `doctor` ✓, `sonar` ✓ Quality Gate **OK** (coverage 82.9%, 0 violations — waiver closed).
- [x] W-006 — Codec matrix fixtures MP3/FLAC/OGG/M4A/Opus/WAV + corrupt-file error path (R-009, R-NFR-05): `scripts/generate-fixtures.sh` + committed `tests/fixtures/` (5 s tones, all with durations) + `codec_matrix` integration tests (each format plays to `Playing` with duration; corrupt input errors loudly and the engine stays usable). Verified: 53 workspace tests ✓, `doctor` ✓, `sonar` ✓ QG OK.
- [x] W-007 — Minimal SQLite open + minimal scan harness (R-006 part): `tunex-library` (`db` with v1 migrations + WAL, `scan` with extension filter/stable-keys/symlink skips) + `tunex-app` end-to-end test (scan fixtures → play WAV to `Playing`). Bridge moved to lib + `run()` so all link targets share one implementation. Verified: 61 workspace tests ✓, `doctor` ✓, `sonar` ✓ QG OK, GUI smoke (shell renders).
- [x] W-008 — MPRIS skeleton (play/pause/next/prev/status) + playerctl smoke (R-013 part): `tunex-app::mpris` (`Server` + full `Root`/`Player` interfaces over local state, tokio runtime on detached thread, Qt keeps main). Verified: 6 unit tests ✓, live-bus smoke (`Identity=TuneX`, Stopped→PlayPause→Playing→PlayPause→Paused via qdbus6; playerctl absent, busctl/qdbus6 cover it), `doctor` ✓, `sonar` ✓ QG OK. Full engine wiring in S5.
- [x] W-009 — CI: native build + qmllint/qmlformat + cargo test + PKGBUILD/`makepkg` check; DoD demo script draft (R-016): `.github/workflows/ci.yml` (Arch container: full `just doctor` gate + CMake build + QML lint/format + printsrcinfo/namcap; YAML-parsed, runs on push) + `scripts/dod-demo.sh` (gate + build + lint + live MPRIS smoke, 5/5 green locally).
- [x] W-010 — S1 gate: deterministic seek test (paused poll lands 1400–1600 ms) closes the last acceptance gap; full rehearsal (`doctor` ✓, `dod-demo.sh` 5/5 ✓, `sonar` ✓ QG OK 84.5%); S1 accepted with two recorded deferrals (full `makepkg` build → S5; `playerctl` → `qdbus6`/`busctl` substitution). R1/R2 feasibility risks closed, no loop-back.

## Done (recent)
- S6 (W-038–W-045) closed 2026-09-13. Implement phase complete. Phase 7 Release next (human confirmation for AUR publish).
